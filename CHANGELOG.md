# 更新日志

本项目遵循语义化版本。第一版从零到 v1.0.0 的完整过程记录在
[docs/decision-log.md](docs/decision-log.md)（19 条决策与验证记录），本文件只保留面向使用者的结论。

## v1.0.0（2026-09-24）—— 首个正式版本

### 系统能力

- **端到端闭环**：需求输入 → 需求解析 → 供应商资质核验 → 逐项比价 → 订单审批 → 逐行下单，全程落到 SQLite 事件流。
- **主 Agent 委派三类子 Agent**：走 DeepAgents 框架原生的 `task` 工具，事件流里标明本次委派是
  `framework` 还是降级为 `fallback`，降级绝不伪装成功。
- **四个页面**：任务看板（选着买 / 说着买两种入口）、采购执行页（三栏 + 实时事件）、
  数据台（供应商能不能用 / 我的订单 + 故障注入）、评测页。
- **人工审批作为持久化中断**：任务挂起后进程重启仍可批准、驳回或要求修改；审批通过前不写订单。
- **澄清闭环**：缺物料或数量时停下来提问，用户补充后从解析阶段续跑，不是重建任务。
- **多物料采购**：一条需求可含多种物料，逐项比价、逐行下单，审批按**整单合计**判定。

### 业务规则（全部确定性，不交给模型）

- 资质核验顺序：黑名单 → 资质缺失 → 经营范围不覆盖 → 缺产品注册证 → 资质过期 → 临期标记，逐物料判定。
- 比价：综合成本 = 单价 × 数量 + 运费；推荐排序为「资质临期 → 交期不满足 → 效期不足 → 未达起订量 → 综合成本」，
  再叠加一层弱偏好——成本差异在 2% 以内时优先 A 类供应商（`tier_preference_tolerance`），改选理由写进结论。
- 审批七条规则：整单金额超阈值、供应商资质临期、非最低价且差额超 10%、可用报价不足、交期不可行、
  低于起订量、到货批次效期不足。

### 演示数据

5 种物料 × 4 家可用供应商的报价矩阵（另加 1 家黑名单供应商，它也有报价，用来演示"报价最低也不可用"）。
中选者刻意分散到三家，每一家、每一个价格都对应一条要演示的规则；老库用幂等补齐逻辑就地升级，不丢历史任务与订单。

### 工程化

- 259 项自动化测试（含结构约束、载荷往返、离线与真机评测用例集）。
- `ruff check .` 零告警；`.github/workflows/ci.yml` 执行「ruff → pytest → 离线评测」。
- 依赖锁定：`pyproject.toml` 给直接依赖定上下界，`requirements.lock.txt` 冻结实测可用的整套版本。
- 可复现评测：30 条常规用例（正常 12 / 单故障 15 / 组合故障 3）+ 13 条高难度用例，每条用例一个独立数据库。

### 实测指标（2026-09-24）

| 指标 | 离线确定性模式 | 真实模型模式 |
| --- | --- | --- |
| 常规用例（30 条） | 30/30，平均约 0.8–0.9 秒 | 30/30，平均 24.9 秒 |
| 高难度用例（13 条） | 13/13 | 13/13，平均 21.5 秒 |
| 人工介入率 | 26.7% | 26.7%（常规）/ 30.8%（高难度） |
| 框架委派 / 降级 | 90 / 0 | 90 / 0（常规）、33 / 0（高难度） |
| 每任务 token | 0（离线无用量，不造假） | 均值 58,941，最大 69,055 |

### 本版修掉的关键缺陷

- 载荷漏字段导致**起订量与效期两条转人工规则在端到端流程中从未触发**（离线单测测不出，因为直接调用了比价函数）。
- 订单接口的物料名只映射了第一种物料，其他物料的订单显示成物料 ID。
- `policy_denied` 事件是死路径（策略护栏未接线），文档却声称可查。
- 同一次审批重复写两条 `approval_decided` 事件。
- 任务完成后"已耗时"持续增长（没读完成时间，之前显示 84761s 的任务实际只跑了 27 秒）。
- 老库补齐只补"缺失的行"、不同步"改过的字段"，导致老库比新建库少一家可用供应商。

### 已知限制

登录与权限体系、真实 ERP 对接、分批到货与阶梯价、并发压测、容器级隔离、跨进程多副本部署均未实现；
整单比价结论仍是各物料理由的拼接，展示层未按物料分行；"下周3"这类数字星期不识别。
完整边界说明见 [PRD 3.2](PRD.md) 与 [docs/handoff.md](docs/handoff.md) 的"已知局限"。
