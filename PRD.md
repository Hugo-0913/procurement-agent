# 耗材采购自动化 Agent 系统 · 产品需求文档（PRD）

**版本：** v1.0
**日期：** 2026-09-17
**状态：** 已与需求方逐条确认，作为后续开发依据
**配套文档：** `plan.md`（实施计划）

---

## 1. 项目背景与目标

### 1.1 背景

企业采购部门在处理耗材采购时，需要在多家供应商报价、供应商资质文件、历史成交价格和内部审批规则之间反复核对。这是一个规则明确、步骤重复、但异常分支很多的流程：资质可能过期、报价可能缺失、金额可能超预算、审批可能被驳回。人工处理的成本主要不在"正常路径"，而在这些异常分支的核对与追溯上。

本项目以该场景为业务外壳，构建一个可演示、可评测的自动化系统。

### 1.2 项目目标

本项目是**求职作品**，目标岗位为 **AI/Agent 应用开发**。核心目标不是复刻企业 ERP，而是以耗材采购为业务外壳，完整展示 Agent 应用工程能力：委派架构、状态机、技能系统、上下文治理、安全边界、可观测性与可复现评测。

面试场景下的成功标准：

1. 5 分钟内可演示完整业务闭环（需求输入 → 资质核验 → 比价分析 → 订单审批 → 完成），全程停留在同一个页面。
2. 可现场演示至少两类异常路径（资质过期、报价不足或超预算），并展示系统的处理过程而非仅展示结果。
3. 所有对外宣称的指标（成功率、人工介入率、耗时、上下文占用）都能由仓库内的评测脚本一键复现。
4. 每个关键技术决策都能回答"为什么这样选、替代方案是什么、代价是什么"。

### 1.3 非目标

本项目不是生产级采购系统，不追求真实 ERP 集成、多租户、高并发与合规审计等级。这些能力在文档中作为"生产化演进方向"说明，不进入 v1 实现范围。

---

## 2. 目标用户与使用场景

### 2.1 用户角色

| 角色 | 说明 | 在本系统中的行为 |
| --- | --- | --- |
| 采购专员（主用户） | 发起采购需求的人 | 输入需求、查看执行过程、在审批环节处置 |
| 审批人 | 金额超阈值时的决策者 | 批准 / 驳回 / 要求修改，需留下理由 |
| 观察者（面试官 / 评审） | 观看演示的人 | 查看任务树、时间线、订单落库结果、评测报告 |

### 2.2 核心场景

| 编号 | 场景 | 说明 |
| --- | --- | --- |
| S1 | 正常采购 | 需求明确、供应商资质齐备、报价充足、金额在阈值内，全流程自动完成 |
| S2 | 资质过期 | 最优供应商资质本月到期，系统改选次优供应商并在结论中说明理由 |
| S3 | 超预算触发审批 | 订单金额超过阈值，任务挂起等待人工审批 |
| S4 | 审批驳回重跑 | 审批人驳回并给出理由，任务回退至比价阶段重新选型 |
| S5 | 长任务上下文治理 | 多轮工具调用后触发自动摘要，控制上下文占用 |

---

## 3. 范围

### 3.1 v1 包含

| 里程碑 | 内容 |
| --- | --- |
| M1 地基 | 项目骨架、DeepSeek 接入、DeepAgents 能力冒烟验证、mock ERP 数据层与故障注入 |
| M2 Agent 核心 | 主 Agent + 三类子 Agent、YAML 配置驱动、流程状态机（含中断与恢复）、四类技能与渐进式加载 |
| M3 治理能力 | 虚拟文件系统、持久化记忆、上下文摘要中间件、双层安全沙箱、可插拔中间件体系、异常捕获与反思重试 |
| M4 Web 界面 | 任务看板页、采购执行页、审批交互、数据台与故障注入、评测页 |
| M5 评测与文档 | 评测脚本与固定用例集、指标报告、架构文档、决策记录、演示脚本 |

### 3.2 v1 明确不做

1. 真实 ERP 对接（SAP / 用友 / 金蝶等）
2. 容器或微 VM 级隔离（在安全设计文档中说明生产化方案）
3. 多租户与真实用户体系、SSO、权限管理后台
4. 支付、收货、入库、发票、对账等下游流程
5. 新供应商引入、招投标、寻源
6. 技能热插拔、技能版本管理、技能市场
7. 向量检索式长期记忆（v1 采用结构化存储 + 规则召回）
8. 并发多任务调度、分布式执行
9. 多模型路由与模型降级策略
10. 移动端适配
11. 性能压测与容量规划

---

## 4. 业务流程与状态机

### 4.1 流程阶段

```
需求输入 → 需求解析 → 资质核验 → 比价分析 → 订单草稿 → 等待审批 → 订单生成 → 完成
                          ↑                        |
                          └────── 驳回重跑 ─────────┘
```

### 4.2 状态定义

| 状态 | 含义 | 终态 |
| --- | --- | --- |
| `PENDING` | 任务已创建，尚未开始 | 否 |
| `PARSING` | 主 Agent 解析需求，产出结构化采购要素 | 否 |
| `QUALIFYING` | 资质核验子 Agent 工作中 | 否 |
| `SOURCING` | 比价分析子 Agent 工作中 | 否 |
| `ORDER_DRAFTING` | 订单执行子 Agent 生成订单草稿 | 否 |
| `AWAITING_APPROVAL` | 命中审批策略，任务挂起，等待人工处置 | 否 |
| `REVISION_REQUIRED` | 审批驳回，等待回退重跑 | 否 |
| `ORDERED` | 订单已写入 mock ERP | 否 |
| `COMPLETED` | 任务正常结束 | 是 |
| `FAILED` | 重试耗尽或不可恢复错误 | 是 |

### 4.3 关键状态转移

| 触发事件 | 起点 | 终点 | 附加动作 |
| --- | --- | --- | --- |
| 提交需求 | `PENDING` | `PARSING` | 创建任务事件流，加载采购偏好记忆 |
| 解析成功 | `PARSING` | `QUALIFYING` | 写入结构化采购要素，激活需求解析技能 |
| 核验完成 | `QUALIFYING` | `SOURCING` | 产出合格供应商清单，不合格项记录原因 |
| 比价完成 | `SOURCING` | `ORDER_DRAFTING` | 产出比价表与推荐理由 |
| 草稿金额未超阈值 | `ORDER_DRAFTING` | `ORDERED` | 直接写入 mock ERP |
| 草稿金额超阈值 | `ORDER_DRAFTING` | `AWAITING_APPROVAL` | 策略引擎拦截，任务挂起并落库 |
| 审批批准 | `AWAITING_APPROVAL` | `ORDERED` | 记录审批人、时间、理由，写入订单 |
| 审批驳回 | `AWAITING_APPROVAL` | `REVISION_REQUIRED` | 记录驳回理由 |
| 驳回后重跑 | `REVISION_REQUIRED` | `SOURCING` | 重新选型，保留上一轮比价结果用于对比 |
| 审批要求修改 | `AWAITING_APPROVAL` | `ORDER_DRAFTING` | 携带审批意见重新生成草稿 |
| 订单写入成功 | `ORDERED` | `COMPLETED` | 产出结果汇总卡片 |
| 重试耗尽 | 任意非终态 | `FAILED` | 记录失败原因与最后错误 |

### 4.4 中断与恢复要求

`AWAITING_APPROVAL` 是一个**持久化挂起状态**，不是进程内阻塞。要求：任务挂起期间进程可以重启，重启后任务仍处于等待审批状态，审批后能从断点继续执行。这一要求直接决定了状态机归属方案。

---

## 5. 功能需求

### FR-01 需求输入与解析

**描述：** 用户以自然语言输入采购需求，系统解析为结构化采购要素。

**结构化要素：** 物料名称与规格、数量、单位、期望交期、预算上限、成本中心、备注。

**规则：**
- 无法解析出的必填要素（物料、数量）时，向用户提问澄清，不得猜测填充。
- 解析结果需在页面上字段级展示，允许人工发现错误。

**验收标准：** 输入"帮我采购 50 箱 A4 纸，下周一到货"能解析出物料、数量、交期三个要素，并在左栏结构化展示。

### FR-02 供应商资质核验

**描述：** 资质核验子 Agent 在候选供应商中筛出可用供应商。

**规则：**
- 核验项：营业执照有效性、行业资质、资质有效期、黑名单状态。
- 资质在有效期内但 30 天内到期，标记为"临期"，不直接淘汰，但必须在比价结论中披露。
- 任一硬性资质缺失或过期，直接淘汰并记录原因。
- 黑名单供应商一律淘汰。

**验收标准：** 候选供应商中若有资质过期者，该供应商不出现在合格清单中，且任务时间线含一条说明淘汰原因的事件。

### FR-03 比价分析

**描述：** 比价分析子 Agent 在合格供应商中比价并给出推荐。

**规则：**
- 比较维度：单价、总价、运费、交期、历史成交价偏差、供应商等级。
- 推荐默认选择综合成本最低者，但需排除"临期资质"供应商。
- 当推荐结果不是最低报价时，必须给出理由（例如价格最低者资质临期）。
- 报价供应商不足 2 家时，标记为"询价不足"，进入降级策略：向用户提示并以唯一报价继续或者终止，由配置决定。

**验收标准：** 比价结果包含至少 2 家供应商对比、推荐结论和推荐理由；结论中不是最低价时理由字段非空。

### FR-04 订单生成与审批拦截

**描述：** 订单执行子 Agent 依据比价结论生成订单草稿，策略引擎判断是否需要人工审批。

**规则：**
- 订单草稿包含：供应商、物料、数量、单价、总额、交期、成本中心。
- 审批触发条件（可配置，v1 默认）：订单总额超过 ¥50,000；供应商为临期资质；比价结果非最低价且差额超过 10%。
- 命中任一条件即挂起等待审批，并在页面上给出命中的规则原文。
- 未命中任何条件则直接写入 mock ERP。

**验收标准：** 金额 ¥62,400 的订单触发审批，页面上显示命中规则"订单金额 ¥62,400 > 阈值 ¥50,000"；金额在阈值内的订单直接完成。

### FR-05 人工审批

**描述：** 审批人在页面上处置挂起的任务。

**规则：**
- 三种处置：批准、驳回、要求修改。驳回与要求修改必须填写理由，批准可选填。
- 审批动作需留痕：操作人、时间、理由、处置类型，全部落库并写入任务时间线。
- 驳回后任务回退至 `SOURCING` 重跑；要求修改后任务回到 `ORDER_DRAFTING`。

**验收标准：** 批准后订单出现在数据台的订单列表中；驳回后任务树中对应节点显示"已驳回重跑"，且重跑后产生新的比价结论。

### FR-06 技能系统与渐进式加载

**描述：** 四类领域技能以 `SKILL.md` 形式存在，按需加载以节省上下文。

**四类技能：**
1. 采购需求解析（`requirement_parsing`）
2. 供应商资质核验（`supplier_qualification`）
3. 比价与降本分析（`price_comparison`）
4. 订单合规与审批（`order_compliance`）

**规则：**
- 每个技能文件包含 frontmatter（`name`、`description`）与正文。
- 主 Agent 初始只可见名称与描述；正文在被判定需要时才加载。
- 加载行为必须可观测：页面上区分"仅描述"与"已加载全文"，时间线记录加载事件。

**验收标准：** 任务运行结束后，页面上四个技能的加载状态与时间线中的加载事件一致；未使用的技能保持"仅描述"状态。

### FR-07 虚拟文件系统

**描述：** Agent 读写文件统一经过虚拟文件系统层，而非直接访问宿主文件系统。

**规则：**
- 提供工作区目录（可读写）与只读目录（含 mock ERP 导出的数据、技能文件）。
- 所有路径先规范化再校验，禁止越出挂载点的路径（含 `..` 与绝对路径逃逸）。
- 文件操作产生事件记录（路径、操作类型、大小）。

**验收标准：** 尝试写出挂载点之外的路径被拒绝，且时间线中记录一条拒绝事件。

### FR-08 持久化记忆

**描述：** 记忆模块沉淀跨任务复用的信息。

**记忆内容：**
- 采购偏好：优先供应商等级、常用成本中心、常用物料规格、金额审批阈值（用户可覆盖）。
- 历史供应商数据：历史成交价、按时交付率、历史驳回次数。

**规则：**
- 记忆在任务开始时加载，并作为提示注入主 Agent 上下文（而非全部历史数据）。
- 任务结束后回写新增事实（例如本次成交价、本次审批结果）。
- 记忆内容需在页面上可查看，避免成为黑盒。

**验收标准：** 第二次采购同一物料时，左栏显示"已加载偏好"，且比价结论引用历史成交价。

### FR-09 上下文摘要中间件

**描述：** 在长任务中自动压缩上下文。

**规则：**
- 触发条件：上下文 token 估算超过设定阈值，或工具调用轮次超过设定值。
- 摘要保留：采购要素、已完成的阶段结论、未决问题；丢弃：完整的工具返回原文。
- 每次触发记录事件：压缩前 token 估算、压缩后 token 估算、丢弃内容类别。

**验收标准：** 在超过阈值的任务中，时间线出现摘要事件并显示前后 token 对比；任务结论不因压缩而丢失关键字段。

### FR-10 双层安全沙箱

**描述：** 所有敏感操作经过两层防护。

**层一 · 策略校验层：** 工具调用前校验——路径白名单、命令白名单、网络禁用、金额阈值、是否需人工审批。拒绝时返回结构化理由，供 Agent 反思重试。

**层二 · 执行隔离层：** 实际执行在受限子进程中——固定工作目录、只读/只写目录分离、环境变量白名单、执行超时、输出大小上限、网络默认关闭。

**规则：**
- 危险命令（删除、格式化、下载、远程执行类）一律拒绝，不做"部分放行"。
- 拒绝事件必须可观测，包含被拒绝的操作与命中规则。

**验收标准：** 尝试执行越权路径写入或危险命令被拒绝，时间线中可见被拦截操作与规则名称。

### FR-11 异常捕获与反思重试

**描述：** 工具调用失败时，捕获异常并带上下文重试。

**规则：**
- 可重试错误：超时、临时性接口失败、结构化输出校验失败。
- 不可重试错误：资质过期导致的业务性失败、策略拒绝（需先修正参数）、参数缺失。
- 重试上限可配置（v1 默认 3 次），每次重试需记录：第几次、失败原因、修正措施。
- 重试耗尽后任务进入 `FAILED` 并保留完整失败上下文。

**验收标准：** 注入接口超时后，任务自动重试并在时间线显示重试次数与原因；重试成功后任务继续完成。

### FR-12 可观测性与任务可视化

**描述：** 任务执行过程需可实时观察。

**规则：**
- 事件实时推送到前端（SSE），任务状态与事件均落库，刷新页面不丢上下文。
- 任务树展示主 Agent → 子 Agent → 工具调用步骤的层级关系与状态。
- 时间线为平铺事件流，支持按 Agent 与事件类型过滤。
- 中间件动作作为一等事件展示（摘要、拦截、重试、技能加载）。

**验收标准：** 任务运行中时间线持续更新；刷新页面后仍能看到完整历史事件。

### FR-13 评测与指标

**描述：** 提供一键运行的评测能力，产出可复现指标。

**规则：**
- 固定用例集（v1 共 30 条），覆盖正常路径与至少 4 类异常路径。
- 每条用例固定随机种子与故障注入配置，保证可复现。
- 输出指标：成功率、人工介入率、平均耗时、上下文 token 峰值与均值。
- 失败用例可跳转至对应任务的执行页回看。

**验收标准：** 在无网络依赖的测试模式下，评测脚本可重复运行并产出稳定结果。

---

## 6. 页面结构与交互

系统共 4 个页面。**任务看板页**与**采购执行页**为核心页面，**数据台**与**评测页**用于证明数据真实落库与指标可复现。

### 6.1 页面一：任务看板页 `/`

**定位：** 入口页，也是"系统在被真实使用"的证据页。

**区块：**
1. 标题栏与状态筛选标签：全部 / 运行中 / 等待审批 / 已完成 / 失败。
2. 新建采购需求区：多行文本输入框 + 提交按钮 + "填入示例需求"链接。
3. 历史任务卡片列表：需求摘要（前 40 字）、状态徽章、创建时间、耗时、人工介入次数标记。

**交互：**
- 点"提交" → 创建任务 → 跳转执行页并自动开始运行。
- 点"填入示例需求" → 自动填充文本框，不提交。
- 点任务卡片 → 进入该任务执行页。

### 6.2 页面二：采购执行页 `/tasks/{id}`

**定位：** 主工作区，演示停留页。三栏布局 + 顶部状态条。

```
┌──────────────────────────────────────────────────────────────┐
│ 需求摘要  [解析]──[核验]──[比价]──[审批]──[完成]  运行中 03:12 │  ← 顶部状态条
├────────────┬──────────────────────────┬──────────────────────┤
│ 任务上下文  │      任务树 / 委派视图     │    执行时间线          │
│ (320px)    │                          │      (420px)         │
│            │   主 Agent                │ [10:02:11] 委派核验    │
│ 需求原文    │    ├ 资质核验 Agent ✓     │ [10:02:13] 工具调用    │
│ 结构化结果  │    ├ 比价分析 Agent ⟳     │ [10:02:20] 摘要触发    │
│ 已加载记忆  │    └ 订单执行 Agent ⏸     │ [10:02:31] 命中规则    │
│ 已激活技能  │                          │                      │
└────────────┴──────────────────────────┴──────────────────────┘
```

**顶部状态条：** 横向阶段指示器（当前高亮、已完成打勾、被驳回标红）、运行状态、已耗时、token 计数。

**左栏 · 任务上下文：**
- 采购需求原文。
- 结构化解析结果（物料 SKU、数量、交期、预算、成本中心），字段级展示。
- 记忆提示区：显示已加载的采购偏好（例如"优先 A 类供应商、单笔超 ¥50,000 需审批"）。
- 技能状态区：四个技能分别显示"已加载全文"（实心）或"仅描述"（空心）。

**中栏 · 任务树：**
- 层级：主 Agent → 三个子 Agent → 各自的工具调用步骤。
- 步骤行内容：状态图标（待执行 / 运行中 / 成功 / 失败重试 / 被拦截）、耗时、一行摘要。
- 失败重试步骤标注重试次数与原因。
- 被拦截步骤整行高亮为橙色并标注"等待审批"。

**中栏交互：**
- 点步骤 → 右栏时间线滚动并高亮对应事件，展开详情（入参、返回、token、重试历史）。
- 点子 Agent 节点 → 折叠 / 展开其下步骤。
- 悬停"仅描述"技能 → 提示该技能触发条件。

**右栏 · 执行时间线：**
- 时间顺序事件流，顶部支持按 Agent 与事件类型过滤。
- 中间件动作显式成事件，例如 `[中间件·上下文摘要] 12.4k → 3.6k tokens`、`[中间件·策略引擎] 命中规则：金额超过阈值`、`[技能] 已加载 price_comparison 全文`。

**审批交互（页内完成，不跳页）：**
- 触发后任务转入"等待审批"，顶部状态条变橙色，中栏对应步骤高亮，右栏顶部滑出审批卡片。
- 卡片内容自上而下：命中的规则原文、订单摘要表（供应商、物料、数量、单价、总额、交期）、比价结论与推荐理由（非最低价时必须显示）、审批意见输入框、三个按钮。
- 点"批准" → 订单写入 mock ERP，任务继续至完成，卡片收起，时间线追加带操作人与时间的审批记录。
- 点"驳回" → 必须填写理由 → 任务回退至比价阶段重跑，任务树对应节点标记"已驳回重跑"。
- 点"要求修改" → 订单执行 Agent 携带审批意见重新生成草稿。

**任务完成：** 不退场，中栏顶部出现结果卡片：订单号、最终供应商、总金额、相比最高报价节省金额、总耗时、人工介入次数。

### 6.3 页面三：数据台 `/data`

**定位：** 证明数据真实落库，并提供异常路径开关。

**区块：**
1. 三个标签页：供应商（资质类型、有效期、状态）、报价（当前询价结果）、订单（含审批记录）。
2. 右上角故障注入面板，开关形式提供："让供应商 B 资质过期"、"让供应商 C 停止报价"、"让全部报价超预算"。

**交互：**
- 打开故障开关 → 回到执行页重跑 → 现场演示异常处理与反思重试。
- 点订单行 → 跳转对应任务的执行页。

### 6.4 页面四：评测页 `/eval`

**定位：** 对外指标的出处。

**区块：** "运行评测"按钮；结果表（用例总数、成功率、人工介入率、平均耗时、上下文 token 峰值与均值）；失败用例列表。

**交互：** 点失败用例 → 跳转对应任务执行页。

### 6.5 页面跳转关系

```
任务看板页 ──提交需求──→ 采购执行页（自动开始）
     │                        │
     └──点历史卡片────────────→┘
                              │
                    顶部导航 ──┼──→ 数据台 ──点订单──→ 采购执行页
                    顶部导航 ──┴──→ 评测页 ──点失败用例──→ 采购执行页
```

执行页内的任务完成、审批、异常处理均不跳页，全部以卡片与事件流呈现。

---

## 7. 数据模型

全部数据存于 SQLite，表如下（v1 最小字段集）。

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| `materials` | `id`, `sku`, `name`, `spec`, `unit`, `category` | 耗材主数据 |
| `suppliers` | `id`, `code`, `name`, `tier`, `blacklisted`, `delivery_rate` | 供应商主数据 |
| `supplier_qualifications` | `id`, `supplier_id`, `qual_type`, `issued_at`, `expires_at` | 资质与有效期 |
| `quotes` | `id`, `supplier_id`, `material_id`, `unit_price`, `freight`, `lead_days`, `valid_until`, `available` | 当前询价 |
| `price_history` | `id`, `supplier_id`, `material_id`, `unit_price`, `ordered_at` | 历史成交价 |
| `tasks` | `id`, `request_text`, `state`, `structured_request`, `created_at`, `updated_at`, `finished_at`, `human_interventions`, `token_usage` | 任务主表 |
| `task_events` | `id`, `task_id`, `seq`, `agent`, `event_type`, `payload`, `created_at` | 事件流（时间线与任务树的数据源） |
| `orders` | `id`, `task_id`, `supplier_id`, `material_id`, `quantity`, `unit_price`, `total_amount`, `lead_days`, `cost_center`, `status`, `created_at` | 订单 |
| `approvals` | `id`, `task_id`, `order_id`, `decision`, `operator`, `reason`, `matched_rules`, `created_at` | 审批留痕 |
| `agent_memory` | `id`, `scope`, `key`, `value`, `updated_at` | 持久化记忆（偏好、历史） |
| `fault_flags` | `id`, `flag`, `enabled`, `updated_at` | 故障注入开关 |

事件类型（`task_events.event_type`）枚举：`stage_change`、`agent_delegation`、`tool_call`、`tool_result`、`skill_loaded`、`policy_denied`、`retry`、`context_summarized`、`approval_requested`、`approval_decided`、`memory_loaded`、`memory_written`、`error`、`task_finished`。

---

## 8. 非功能需求

### 8.1 运行环境

- 操作系统：Windows 11；Python 3.11。
- 本机无 Docker、无可用 WSL，因此沙箱为进程级实现（见 FR-10）。
- 不引入需要 npm 构建的前端工具链（本机 Node 版本过旧），前端为原生 HTML/CSS/JS。

### 8.2 模型与外部依赖

- 模型：DeepSeek（OpenAI 兼容接口），v1 仅使用单一模型，不做多模型路由。
- 所有 LLM 调用必须可替换、可注入，以便测试在无网络条件下运行。

### 8.3 安全

- 敏感写操作必须经过策略校验与审批拦截。
- 路径穿越、危险命令、网络访问默认拒绝。
- 密钥通过环境变量注入，禁止写入仓库。

### 8.4 可观测性

- 所有阶段变更、委派、工具调用、中间件动作、审批动作均可查询与追溯。
- 事件持久化，页面刷新不丢上下文。

### 8.5 可维护性

- 单一职责的文件划分，避免超大文件。
- 关键决策写入决策记录文档。
- 测试覆盖核心状态机、策略引擎、路径校验、重试逻辑。

---

## 9. 指标定义与验收

所有指标由评测脚本产出，禁止手工填写。

| 指标 | 定义 | 计量方式 |
| --- | --- | --- |
| 任务成功率 | 达到 `COMPLETED` 的用例数 / 总用例数 | 评测脚本统计 |
| 人工介入率 | 触发过人工审批的用例数 / 总用例数 | 评测脚本统计 |
| 平均耗时 | 从 `PENDING` 到终态的平均墙钟时间（评测模式为加速模拟） | 评测脚本统计 |
| 上下文占用 | 单任务上下文 token 估算峰值与均值 | 摘要中间件与模型调用层记录 |
| 重试改善度 | 存在重试的用例中，重试后成功的比例 | 评测脚本统计 |

评测模式下的耗时口径需在评测报告中明确说明（模拟时钟与真实墙钟的区别），避免指标被误读。

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| DeepSeek 工具调用稳定性不足 | 子 Agent 委派失败、循环调用 | M1 先做冒烟验证；子 Agent 职责切窄、限制单轮工具调用数、结构化输出校验、重试上限 |
| 框架 API 与文档版本不一致 | 开发返工 | 冒烟任务中锁定版本号并记录到决策文档 |
| 演示现场异常 | 演示失败 | 固定示例需求、固定随机种子、预置故障注入开关、提供离线回放模式 |
| 上下文压缩丢失关键信息 | 结论错误 | 压缩后校验采购要素与阶段结论完整性，缺失则重新取回 |
| 范围蔓延 | 交付延期 | 严格遵守 3.2 的"不做清单"，新增需求记入 v2 待办而非插入 v1 |
| 指标不可复现 | 面试被质疑 | 评测脚本 + 固定用例集 + 固定种子，结果写入报告文件 |

---

## 11. 术语表

| 术语 | 含义 |
| --- | --- |
| 主 Agent | 采购协调者，负责解析需求、委派子任务、汇总结论 |
| 子 Agent | 承担单一职责的执行单元：资质核验、比价分析、订单执行 |
| 技能（Skill） | 以文件形式存在的领域知识包，按需加载 |
| 渐进式加载 | 先只暴露技能名称与描述，需要时才加载正文 |
| 任务树 | 主 Agent 与子 Agent、工具调用步骤的层级结构 |
| 时间线 | 按时间排序的平铺事件流 |
| 策略引擎 | 敏感操作的第一层校验，决定放行、拒绝或转人工 |
| 反思重试 | 工具调用失败后，携带失败上下文重新尝试 |

